# Cloudflare Pages / Netlify rewrites.
# When Oqra IS installed, iOS intercepts https://oqra.app/p/<code> before the
# browser loads anything. This rewrite only fires when the app is NOT installed:
# it serves the friendly fallback page for any /p/<code> link (200, not a 301).
/p/*  /p/index.html  200
