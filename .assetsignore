# Files in the repo that must NOT be published as website assets.
# (Cloudflare Workers static assets reads this, gitignore-style.)
.git
.gitignore
.wrangler
wrangler.jsonc
wrangler.toml
README.md
*.log
.DS_Store
# Source photos (DNG originals + exported HEIC looks) — heavy, not for the web.
# The web-ready squares live in assets/img/samples/ and DO publish.
assets/SamplePhotos/
